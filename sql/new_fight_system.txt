ALTER TABLE `skill`
  DROP `pl_base_dmg`,
  DROP `dmg`,
  DROP `pl_dmg`,
  DROP `range_min`,
  DROP `range_max`,
  DROP `pl_str`,
  DROP `pl_con`,
  DROP `pl_dex`,
  DROP `pl_int`,
  DROP `pl_sag`,
  DROP `pl_res`,
  DROP `pl_mana`,
  DROP `pl_effect_pourcent`;
  
  CREATE TABLE `fights` (`id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY, `ready_phase` TINYINT NOT NULL, `turn` SMALLINT NOT NULL, `turn_id` SMALLINT NOT NULL, `locked` TINYINT NOT NULL, `is_end` TINYINT NOT NULL, `time` SMALLINT NOT NULL, `pvp` TINYINT NOT NULL, `timestamp` INT NOT NULL, `char_associated` INT NOT NULL) ENGINE = MyISAM;
  
  CREATE TABLE `fighters` (
`fighter_id` INT NOT NULL ,
`fight_id` INT NOT NULL ,
`is_char` TINYINT NOT NULL ,
`is_ready` TINYINT NOT NULL ,
`needRefresh` TINYINT NOT NULL ,
`team` TINYINT NOT NULL ,
`pa` TINYINT NOT NULL ,
`exp_win` SMALLINT NOT NULL ,
`gold_win` SMALLINT NOT NULL
) ENGINE = MYISAM ;

